# Web-React 功能对等迁移 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增与现网功能对等的 React 前端 `web-react/`（CSS Modules），保留 `web/` 不变；Tauri 改为指向 `web-react`。

**Architecture:** Vite + React SPA，端口 5174，代理 `/v1`。状态拆成 Auth / Chat / Theme providers；路由用手写 `history` API。样式用 `tokens.css` + 各组件 `*.module.css`。纯逻辑从 `web/src` 复制到 `web-react/src/lib`。

**Tech Stack:** React 19、react-dom、@vitejs/plugin-react、TypeScript、Vite 7、vitest、现有 marked/dompurify、Tauri 2

**Spec:** `docs/superpowers/specs/2026-07-12-web-react-design.md`

---

## 硬约束：禁止再做成庞大 main

| 规则 | 限制 |
|------|------|
| `main.tsx` | 仅 `createRoot` + 挂载，目标 ≤30 行 |
| `App.tsx` | 仅 Providers + 路由切换，目标 ≤100 行 |
| 任意单个 `.ts` / `.tsx` | 软上限 **200 行**；超过 **250 行** 必须再拆文件后再继续 |
| 页面文件 | 只编排布局与绑定；业务逻辑放 hooks / api / lib |
| CSS | 禁止再出现单文件 400+ 行全局样式；按组件 module 拆分 |

对照反例：`web/src/main.ts`（964 行）——`web-react` 不得复刻这种结构。

---

## File structure

| Path | Responsibility |
|------|----------------|
| `web-react/index.html` | 挂载点 + 主题防闪烁脚本 |
| `web-react/vite.config.ts` | React plugin、5174、proxy、`--debug` → `VITE_DEBUG` |
| `web-react/tsconfig.json` | JSX + DOM lib，供独立 typecheck |
| `web-react/src/main.tsx` | 入口挂载 |
| `web-react/src/App.tsx` | Provider 树 + 路由出口 |
| `web-react/src/vite-env.d.ts` | `VITE_*` 类型 |
| `web-react/src/types.ts` | User / Conversation / Turn / StreamEvent |
| `web-react/src/styles/tokens.css` | 设计变量（亮/暗） |
| `web-react/src/styles/global.css` | 仅 reset + font import（短） |
| `web-react/src/lib/choice-prompt.ts` | 自 `web/src/choice-prompt.ts` 复制 |
| `web-react/src/lib/debug-timeline.ts` | 自 `web/src/debug-panel.ts` 复制，**去掉** `renderHtml` |
| `web-react/src/api/client.ts` | `apiUrl`、token 存取、`api()`、401 回调钩子 |
| `web-react/src/api/stream.ts` | `streamApi()` NDJSON |
| `web-react/src/hooks/useRouter.ts` | path、navigate、popstate |
| `web-react/src/context/ThemeContext.tsx` | 主题 |
| `web-react/src/context/AuthContext.tsx` | 用户会话 |
| `web-react/src/context/ChatContext.tsx` | 会话与发消息 |
| `web-react/src/context/ToastContext.tsx` | Toast |
| `web-react/src/pages/LandingPage.tsx` + `.module.css` | 落地页 |
| `web-react/src/pages/AuthPage.tsx` + `.module.css` | 登录/注册 |
| `web-react/src/pages/ChatPage.tsx` + `.module.css` | 聊天编排 |
| `web-react/src/pages/SettingsPage.tsx` + `.module.css` | 设置编排 |
| `web-react/src/components/AppShell.tsx` | 侧栏布局壳 |
| `web-react/src/components/Sidebar.tsx` | 会话列表 |
| `web-react/src/components/MessageList.tsx` | 消息列表 |
| `web-react/src/components/MessageBubble.tsx` | 单条气泡（流式友好） |
| `web-react/src/components/Composer.tsx` | 输入框 |
| `web-react/src/components/Markdown.tsx` | marked + DOMPurify |
| `web-react/src/components/ChoiceDialog.tsx` | 澄清弹窗 |
| `web-react/src/components/ConfirmDialog.tsx` | 确认框 |
| `web-react/src/components/ThemeToggle.tsx` | 主题按钮 |
| `web-react/src/components/DebugPane.tsx` | Debug 侧栏 |
| `web-react/src/components/ConversationMenu.tsx` | 右键菜单 |
| `tests/web-react-choice-prompt.test.ts` | 指向 web-react lib |
| `tests/web-react-debug-timeline.test.ts` | 指向 web-react lib |
| `package.json` | react 依赖与 `web:react*` 脚本；`typecheck` 含 web-react |
| `src-tauri/tauri.conf.json` | 指向 web-react :5174 |

**Do not modify:** `web/**`（本计划零改动 vanilla 前端）。

---

### Task 1: 脚手架 + React 依赖 + Tauri 指向

**Files:**
- Create: `web-react/index.html`, `web-react/vite.config.ts`, `web-react/tsconfig.json`, `web-react/src/main.tsx`, `web-react/src/App.tsx`, `web-react/src/vite-env.d.ts`, `web-react/src/styles/global.css`, `web-react/src/styles/tokens.css`
- Modify: `package.json`, `src-tauri/tauri.conf.json`, `tsconfig.json`（根 typecheck 可保持不动，改用 `typecheck` 串联）

- [ ] **Step 1: 安装依赖**

Run:

```bash
npm install react react-dom
npm install -D @types/react @types/react-dom @vitejs/plugin-react
```

Expected: `package.json` 出现上述依赖，lockfile 更新。

- [ ] **Step 2: 写 `web-react/vite.config.ts`**

```ts
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";
import { fileURLToPath, URL } from "node:url";

const debug = process.argv.includes("--debug");
const debugIndex = process.argv.indexOf("--debug");
if (debugIndex !== -1) process.argv.splice(debugIndex, 1);

export default defineConfig({
  root: fileURLToPath(new URL(".", import.meta.url)),
  plugins: [react()],
  define: {
    "import.meta.env.VITE_DEBUG": JSON.stringify(debug ? "true" : "false"),
  },
  server: {
    host: "127.0.0.1",
    port: 5174,
    proxy: {
      "/health": "http://127.0.0.1:3000",
      "/v1": "http://127.0.0.1:3000",
    },
  },
  build: { outDir: "dist", emptyOutDir: true },
});
```

- [ ] **Step 3: 写 `web-react/tsconfig.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "isolatedModules": true,
    "allowImportingTsExtensions": true,
    "types": ["vite/client"]
  },
  "include": ["src/**/*"]
}
```

- [ ] **Step 4: 写 `index.html`、`vite-env.d.ts`、最小 `App` / `main`**

`web-react/index.html`：复制 `web/index.html`，把 script 改为 `/src/main.tsx`，保留主题防闪烁脚本。

`web-react/src/vite-env.d.ts`：与 `web/src/vite-env.d.ts` 相同。

`web-react/src/main.tsx`：

```tsx
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./styles/global.css";
import "./styles/tokens.css";

const root = document.querySelector("#app");
if (!root) throw new Error("找不到应用挂载点");
createRoot(root).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

`web-react/src/App.tsx`（临时占位，后续任务替换）：

```tsx
export default function App() {
  return <main style={{ padding: 24 }}>Missy React scaffold</main>;
}
```

`tokens.css`：至少定义：

```css
:root {
  --color-bg: #f5f4ef;
  --color-text: #252622;
  --color-accent: #df604c;
  --color-muted: #70716b;
  --font-sans: "DM Sans", "Noto Sans SC", sans-serif;
}
html[data-theme="dark"] {
  --color-bg: #171816;
  --color-text: #e7e7e2;
  --color-muted: #a2a39c;
}
```

`global.css`：font import + `body { margin:0; font-family: var(--font-sans); background: var(--color-bg); color: var(--color-text); }`。后续组件样式再补全 token。

- [ ] **Step 5: 更新 `package.json` 脚本**

在 `scripts` 中加入：

```json
"web:react": "vite --config web-react/vite.config.ts",
"web:react:build": "vite build --config web-react/vite.config.ts",
"web:react:preview": "vite preview --config web-react/vite.config.ts",
"typecheck": "tsc --noEmit && tsc --noEmit -p web-react/tsconfig.json"
```

（覆盖原 `typecheck` 值，串联根与 web-react。）

- [ ] **Step 6: 更新 `src-tauri/tauri.conf.json`**

```json
"build": {
  "beforeDevCommand": "npm run web:react",
  "devUrl": "http://127.0.0.1:5174",
  "beforeBuildCommand": "npm run web:react:build",
  "frontendDist": "../web-react/dist"
}
```

- [ ] **Step 7: 验证脚手架**

Run:

```bash
npm run web:react:build
npm run typecheck
```

Expected: 构建产出 `web-react/dist/`；typecheck 通过。

- [ ] **Step 8: Commit**

```bash
git add package.json package-lock.json web-react src-tauri/tauri.conf.json
git commit -m "$(cat <<'EOF'
chore: scaffold web-react and point Tauri at it

Add Vite+React app on :5174 alongside vanilla web; desktop shell loads web-react.
EOF
)"
```

---

### Task 2: types + lib 复制 + 单测

**Files:**
- Create: `web-react/src/types.ts`, `web-react/src/lib/choice-prompt.ts`, `web-react/src/lib/debug-timeline.ts`
- Create: `tests/web-react-choice-prompt.test.ts`, `tests/web-react-debug-timeline.test.ts`

- [ ] **Step 1: 写失败测试（指向尚不存在的路径）**

`tests/web-react-choice-prompt.test.ts`：复制 `tests/choice-prompt.test.ts`，把 import 改为：

```ts
import { parseChoicePrompt, visibleAssistantContent } from "../web-react/src/lib/choice-prompt.ts";
```

`tests/web-react-debug-timeline.test.ts`：复制 `tests/debug-panel.test.ts`，import 改为：

```ts
import { DebugTimeline, type ClientDebugEvent } from "../web-react/src/lib/debug-timeline.ts";
```

并删除任何对 `renderHtml` 的断言（新文件不导出 HTML 渲染）。

- [ ] **Step 2: 跑测确认失败**

Run: `npm test -- tests/web-react-choice-prompt.test.ts tests/web-react-debug-timeline.test.ts`

Expected: FAIL（模块不存在）。

- [ ] **Step 3: 复制并调整实现**

```bash
cp web/src/choice-prompt.ts web-react/src/lib/choice-prompt.ts
cp web/src/debug-panel.ts web-react/src/lib/debug-timeline.ts
```

编辑 `debug-timeline.ts`：删除 `renderHtml` 方法整段；保留 `DebugTimeline`、`ClientDebugEvent`、`isDebugBuild`。文件应远小于 100 行。

`types.ts`：

```ts
export type Usage = {
  inputTokens: number | null;
  outputTokens: number | null;
  totalTokens: number | null;
};
export type User = {
  id: string;
  email: string;
  displayName: string;
  didaTokenConfigured: boolean;
  didaTokenHint: string | null;
};
export type Conversation = {
  id: string;
  title: string;
  usage: Usage;
  createdAt: string;
  updatedAt: string;
};
export type Turn = {
  id: string;
  userContent: string;
  assistantContent: string | null;
  status: "pending" | "succeeded" | "failed";
  errorMessage?: string | null;
  feedback?: "like" | "dislike" | null;
  usage: Usage;
  createdAt: string;
};
export type ClientDebugEvent =
  | { kind: "phase"; phase: string; status: string; detail?: string }
  | { kind: "thinking"; delta: string }
  | { kind: "tool_call"; name: string; args?: unknown; id?: string }
  | { kind: "tool_result"; name: string; ok: boolean; preview: string; id?: string }
  | { kind: "mcp"; action: string; detail?: string }
  | { kind: "note"; message: string };

export type StreamEvent =
  | { type: "start"; turn: Turn }
  | { type: "delta"; delta: string; reset?: boolean }
  | { type: "done"; turn: Turn }
  | { type: "debug"; event: ClientDebugEvent }
  | { type: "error"; error: { message?: string; code?: string; stack?: string; cause?: string } };
```

若 `debug-timeline.ts` 已导出 `ClientDebugEvent`，则 `types.ts` 可 `export type { ClientDebugEvent } from "./lib/debug-timeline.ts"`，避免重复——二选一，保持单一来源。

- [ ] **Step 4: 跑测通过**

Run: `npm test -- tests/web-react-choice-prompt.test.ts tests/web-react-debug-timeline.test.ts`

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add web-react/src/types.ts web-react/src/lib tests/web-react-*.test.ts
git commit -m "feat(web-react): copy choice-prompt and debug timeline libs"
```

---

### Task 3: API 客户端与流式

**Files:**
- Create: `web-react/src/api/auth-storage.ts`, `web-react/src/api/client.ts`, `web-react/src/api/stream.ts`
- Create: `tests/web-react-auth-storage.test.ts`

- [ ] **Step 1: 写 auth-storage 单测**

```ts
// tests/web-react-auth-storage.test.ts
import { afterEach, describe, expect, it } from "vitest";
import { clearAuthToken, readAuthToken, writeAuthToken } from "../web-react/src/api/auth-storage.ts";

afterEach(() => clearAuthToken());

describe("auth-storage", () => {
  it("reads and writes missy.authToken", () => {
    expect(readAuthToken()).toBeNull();
    writeAuthToken("abc");
    expect(readAuthToken()).toBe("abc");
    clearAuthToken();
    expect(readAuthToken()).toBeNull();
  });
});
```

- [ ] **Step 2: 跑测确认失败 → 实现 `auth-storage.ts`**

```ts
const KEY = "missy.authToken";

export function readAuthToken(): string | null {
  try { return localStorage.getItem(KEY); } catch { return null; }
}
export function writeAuthToken(token: string): void {
  try { localStorage.setItem(KEY, token); } catch { /* ignore */ }
}
export function clearAuthToken(): void {
  try { localStorage.removeItem(KEY); } catch { /* ignore */ }
}
```

Run: `npm test -- tests/web-react-auth-storage.test.ts` → PASS。

- [ ] **Step 3: 实现 `client.ts`**

```ts
import { clearAuthToken, readAuthToken } from "./auth-storage.ts";

export const API_BASE = (import.meta.env.VITE_API_BASE ?? "").replace(/\/$/, "");

export function apiUrl(path: string): string {
  return `${API_BASE}${path}`;
}

export type UnauthorizedHandler = () => void;
let onUnauthorized: UnauthorizedHandler | null = null;
export function setUnauthorizedHandler(handler: UnauthorizedHandler | null): void {
  onUnauthorized = handler;
}

export function authHeaders(extra: HeadersInit = {}): HeadersInit {
  const headers = new Headers(extra);
  const token = readAuthToken();
  if (token) headers.set("Authorization", `Bearer ${token}`);
  return headers;
}

export async function api<T>(url: string, options: RequestInit = {}): Promise<T> {
  const response = await fetch(apiUrl(url), {
    ...options,
    headers: options.body
      ? authHeaders({ "Content-Type": "application/json", ...(options.headers as Record<string, string>) })
      : authHeaders(options.headers),
  });
  if (response.status === 204) return undefined as T;
  if (response.status === 401) {
    const data = await response.json().catch(() => ({})) as { error?: { message?: string; code?: string } };
    if (data.error?.code === "UNAUTHORIZED" || !data.error?.code) {
      clearAuthToken();
      onUnauthorized?.();
    }
    throw new Error(data.error?.message || `请求失败（${response.status}）`);
  }
  const data = await response.json().catch(() => ({})) as T & { error?: { message?: string } };
  if (!response.ok) throw new Error(data.error?.message || `请求失败（${response.status}）`);
  return data;
}
```

- [ ] **Step 4: 实现 `stream.ts`**

从 `web/src/main.ts` 的 `streamApi` 迁出，改为 import `apiUrl` / `authHeaders` / `clearAuthToken` / `onUnauthorized` 模式（401 时同样 `clearAuthToken` + `onUnauthorized?.()`）。类型用 `StreamEvent` from `../types.ts`。目标文件 ≤120 行。

- [ ] **Step 5: Commit**

```bash
git add web-react/src/api tests/web-react-auth-storage.test.ts
git commit -m "feat(web-react): add API client and NDJSON stream helper"
```

---

### Task 4: Router、Theme、Toast

**Files:**
- Create: `web-react/src/hooks/useRouter.ts`, `web-react/src/lib/desktop.ts`
- Create: `web-react/src/context/ThemeContext.tsx`, `web-react/src/context/ToastContext.tsx`
- Create: `web-react/src/components/ThemeToggle.tsx`, `ThemeToggle.module.css`
- Create: `web-react/src/components/ToastHost.tsx`, `ToastHost.module.css`

- [ ] **Step 1: `desktop.ts` + `useRouter.ts`**

```ts
// lib/desktop.ts
export function isDesktopShell(): boolean {
  return "__TAURI_INTERNALS__" in window || "__TAURI__" in window;
}
```

```ts
// hooks/useRouter.ts
import { useCallback, useEffect, useState } from "react";

export function currentPath(): string {
  return window.location.pathname.replace(/\/+$/, "") || "/";
}

export function useRouter() {
  const [path, setPath] = useState(currentPath);
  useEffect(() => {
    const onPop = () => setPath(currentPath());
    window.addEventListener("popstate", onPop);
    return () => window.removeEventListener("popstate", onPop);
  }, []);
  const navigate = useCallback((next: string, replace = false) => {
    if (currentPath() === next) {
      setPath(next);
      return;
    }
    if (replace) history.replaceState(null, "", next);
    else history.pushState(null, "", next);
    setPath(next);
  }, []);
  return { path, navigate };
}
```

- [ ] **Step 2: ThemeContext + ThemeToggle**

行为对齐 vanilla：`missy.theme` localStorage；`document.documentElement.dataset.theme`；更新 `meta[name=theme-color]`。导出 `useTheme()` → `{ theme, toggleTheme, setTheme }`。

- [ ] **Step 3: ToastContext**

```tsx
// 提供 showToast(message, error?: boolean)；ToastHost 渲染一条 toast，3.2s 自动消失
```

- [ ] **Step 4: 在 `App.tsx` 包裹 Theme + Toast，临时仍显示 scaffold 文案 + ThemeToggle 验证切换。

- [ ] **Step 5: Commit**

```bash
git add web-react/src/hooks web-react/src/lib/desktop.ts web-react/src/context web-react/src/components/ThemeToggle* web-react/src/components/ToastHost* web-react/src/App.tsx
git commit -m "feat(web-react): add router, theme, and toast foundations"
```

---

### Task 5: AuthProvider + Landing + Auth 页

**Files:**
- Create: `web-react/src/context/AuthContext.tsx`
- Create: `web-react/src/pages/LandingPage.tsx`, `LandingPage.module.css`
- Create: `web-react/src/pages/AuthPage.tsx`, `AuthPage.module.css`
- Create: `web-react/src/components/BootScreen.tsx`, `BootScreen.module.css`
- Modify: `web-react/src/App.tsx`

- [ ] **Step 1: AuthContext**

职责：
- `user`、`bootstrapping`
- `bootstrap()`：有 token 则 `GET /v1/me`，失败则清会话
- `login` / `register`：写 token，设 user
- `logout`：`POST /v1/auth/logout`（忽略错误）+ 清本地
- `setUser`：设置页更新资料后用
- `clearSession`：供 401 handler
- mount 时 `setUnauthorizedHandler(() => clearSession())`

保持文件 ≤200 行。

- [ ] **Step 2: LandingPage / AuthPage**

视觉用 CSS Modules + tokens；信息架构对齐 vanilla（hero、功能区、登录表单字段）。桌面壳下 Auth 页不显示「返回主页」按钮（`isDesktopShell()`）。

- [ ] **Step 3: App 路由**

```tsx
// 伪结构 — 实际代码写完整
function Routes() {
  const { path, navigate } = useRouter();
  const { user, bootstrapping } = useAuth();
  if (bootstrapping) return <BootScreen />;
  if (!user) {
    if (path === "/login" || path === "/register") return <AuthPage mode={...} />;
    if (isDesktopShell()) { /* replace to /login */ return <AuthPage mode="login" />; }
    return <LandingPage />;
  }
  if (path === "/settings") return <SettingsPage />; // Task 9 前可先占位
  return <ChatPage />; // Task 6/7 前可占位 "chat coming"
}
```

未实现的页面用短占位组件，但路由分支先写好。

- [ ] **Step 4: 手工验证**

Run: `npm run serve`（另一终端）+ `npm run web:react`  
验证：落地页 → 注册/登录 → 能拿到 user（聊天可占位）。

- [ ] **Step 5: Commit**

```bash
git add web-react/src/context/AuthContext.tsx web-react/src/pages web-react/src/components/BootScreen* web-react/src/App.tsx
git commit -m "feat(web-react): add auth flow, landing, and login/register pages"
```

---

### Task 6: ChatContext（会话 + 流式，无大 UI）

**Files:**
- Create: `web-react/src/context/ChatContext.tsx`
- Create: `web-react/src/hooks/useDebugTimeline.ts`（可选，若 debug 状态放这里）

- [ ] **Step 1: 实现 ChatContext**

API 表面：
- `conversations`, `active`, `turns`, `pending`
- `loadConversations`, `createConversation`, `openConversation`
- `renameConversation`, `deleteConversation`, `clearAllConversations`
- `sendMessage(text)`
- `setTurnFeedback(turnId, feedback)`
- `debugTimeline` + `clearDebug`（若 `isDebugBuild()`）

`sendMessage` 逻辑从 vanilla 迁出：
1. 无 active 先 create
2. optimistic pending turn
3. `streamApi` 处理 start/delta/done/debug/error
4. delta 时用函数式 `setTurns` 只改最后一项 `assistantContent`
5. finally `pending=false`，刷新 conversations

**文件必须 ≤250 行**；若超限，拆 `chatActions.ts`（纯 async 函数接收 setters）或 `useSendMessage.ts`。

- [ ] **Step 2: typecheck**

Run: `npx tsc --noEmit -p web-react/tsconfig.json`  
Expected: PASS。

- [ ] **Step 3: Commit**

```bash
git add web-react/src/context/ChatContext.tsx web-react/src/hooks
git commit -m "feat(web-react): add ChatContext with NDJSON streaming"
```

---

### Task 7: 聊天 UI（壳、侧栏、消息、输入）

**Files:**
- Create: `AppShell.tsx` + css, `Sidebar.tsx` + css, `MessageList.tsx` + css, `MessageBubble.tsx` + css, `Composer.tsx` + css, `Markdown.tsx` + css, `ConversationMenu.tsx` + css
- Create: `pages/ChatPage.tsx` + css
- Modify: `App.tsx` 接上 ChatProvider + ChatPage

- [ ] **Step 1: Markdown 组件**

```tsx
import DOMPurify from "dompurify";
import { marked } from "marked";
// 渲染 sanitized HTML；prose 样式在 Markdown.module.css
```

- [ ] **Step 2: MessageBubble**

`memo` 包裹；user 文本 escape；assistant 用 Markdown；pending 无内容显示 typing；succeeded 显示 feedback 按钮（回调 props）。

- [ ] **Step 3: MessageList / Composer / Sidebar / AppShell**

Sidebar：列表、新建、右键菜单触发 rename/delete（delete 用 ConfirmDialog，Task 8 可先用 `window.confirm` 占位并在 Task 8 替换——**优先直接做 ConfirmDialog 若已实现**）。

AppShell：sidebar 折叠（`missy.sidebarCollapsed`）、移动端 open class。

- [ ] **Step 4: ChatPage 编排**

只组装：header（标题、ThemeToggle、profile→settings）、MessageList、token banner、Composer、可选 DebugPane 槽。目标 ≤150 行。

- [ ] **Step 5: 手工验证流式打字是否流畅**

Run API + `npm run web:react`，发一条消息，确认 delta 更新不卡顿。

- [ ] **Step 6: Commit**

```bash
git add web-react/src/components web-react/src/pages/ChatPage.* web-react/src/App.tsx web-react/src/context
git commit -m "feat(web-react): add chat shell, messages, and composer"
```

---

### Task 8: ConfirmDialog + ChoiceDialog

**Files:**
- Create: `ConfirmDialog.tsx` + css, `ChoiceDialog.tsx` + css
- Modify: Sidebar/ChatPage 接入

- [ ] **Step 1: ConfirmDialog**

Props：`{ open, title, message, confirmLabel?, onConfirm, onCancel }`  
行为：Escape 取消、backdrop 点击取消、Tab 在两按钮间循环、打开时 focus 取消按钮。

可做成受控组件 + `useConfirm()` hook 返回 `confirm(opts) => Promise<boolean>`（hook 单独文件 `hooks/useConfirm.ts`）。

- [ ] **Step 2: ChoiceDialog**

用 `parseChoicePrompt` / `visibleAssistantContent`；modes：single / multiple / form；提交后 `sendMessage` 拼装文案与 vanilla 一致；dismiss 记 `dismissedChoiceTurnId`。

- [ ] **Step 3: MessageList 对 assistant 只显示 `visibleAssistantContent`**

- [ ] **Step 4: Commit**

```bash
git add web-react/src/components/ConfirmDialog* web-react/src/components/ChoiceDialog* web-react/src/hooks/useConfirm.ts web-react/src/pages/ChatPage.*
git commit -m "feat(web-react): add confirm and choice dialogs"
```

---

### Task 9: Settings 页

**Files:**
- Create: `pages/SettingsPage.tsx` + css
- Create: `components/settings/ProfileForm.tsx`, `PasswordForm.tsx`, `TokenForm.tsx`, `AccountActions.tsx`（各带 module css）
- Modify: App 路由已指向 SettingsPage

- [ ] **Step 1: 拆成子表单组件**（禁止 SettingsPage 超过 200 行）

校验文案对齐 vanilla：
- 显示名称 / 邮箱
- 密码 ≥8
- Token ≥8

成功 Toast「保存成功」；`setUser` 更新 AuthContext。

- [ ] **Step 2: 清除会话 / 退出 / 注销**

清除与删除对话用 ConfirmDialog；注销可用 `prompt` 要密码（与 vanilla 一致）或小型 PasswordPrompt 组件。

- [ ] **Step 3: Commit**

```bash
git add web-react/src/pages/SettingsPage.* web-react/src/components/settings
git commit -m "feat(web-react): add settings page with account forms"
```

---

### Task 10: Debug 面板

**Files:**
- Create: `components/DebugPane.tsx` + css
- Modify: ChatPage、ChatContext（确保 debug 事件写入 timeline）

- [ ] **Step 1: DebugPane**

仅 `isDebugBuild()` 时渲染；展示 entries + error；清空/折叠；折叠状态 `missy.debugPanelCollapsed`；header DEBUG 角标切换。

用 React 列表渲染，**不要**恢复 `renderHtml`。

- [ ] **Step 2: 验证**

Run: `npm run web:react -- --debug`  
发消息应看到 phase/thinking/tool 事件。

- [ ] **Step 3: Commit**

```bash
git add web-react/src/components/DebugPane* web-react/src/pages/ChatPage.*
git commit -m "feat(web-react): add debug timeline pane"
```

---

### Task 11: 补齐样式 token、对等打磨、验收

**Files:**
- Modify: `styles/tokens.css` 与各 `*.module.css`（按验收缺口补）
- Modify: 任何遗漏的交互（反馈按钮、suggestions、token banner）

- [ ] **Step 1: 对照 spec 验收清单逐项勾选**

手工清单：
1. `npm run web` 仍为 vanilla :5173  
2. `npm run web:react` → :5174  
3. 落地 / 登录 / 注册  
4. 会话新建、打开、重命名、删除  
5. 流式回复 + feedback  
6. Choice 弹窗  
7. 设置全表单 + 清会话 + 退出 + 注销  
8. 主题切换持久化  
9. sidebar 折叠  
10. `--debug` Debug 面板  
11. `npm run web:react:build` 成功  
12. `npm run typecheck` 成功  
13. `npm test`（含 web-react 新测）成功  

- [ ] **Step 2: 行数审计**

Run:

```bash
find web-react/src -name '*.ts' -o -name '*.tsx' | xargs wc -l | sort -n
```

Expected: 无单文件 >250 行；`main.tsx` / `App.tsx` 仍短小。若超限，拆文件后再提交。

- [ ] **Step 3: Commit**

```bash
git add web-react
git commit -m "feat(web-react): polish styles and complete feature parity pass"
```

---

### Task 12: 文档同步（可选短提交）

**Files:**
- Modify: `README.md`（若存在桌面/web 说明）：注明 `web:react` 与 Tauri 现指向 web-react；`npm run web` 仍为 vanilla

- [ ] **Step 1: 更新 README 相关段落**
- [ ] **Step 2: Commit** `docs: note web-react scripts and Tauri frontend switch`

---

## Spec coverage check

| Spec 项 | Task |
|---------|------|
| 并行 web/ + web:react 脚本 | 1 |
| CSS Modules + tokens | 1, 5, 7, 9, 11 |
| lib 复制 + 单测 | 2 |
| API / stream / Bearer | 3 |
| 路由 / 主题 / Toast | 4 |
| Auth + Landing | 5 |
| 流式聊天 | 6–7 |
| Choice / Confirm | 8 |
| Settings | 9 |
| Debug | 10 |
| Tauri 指向 web-react | 1 |
| 不做庞大 main | 全文硬约束 + Task 11 行数审计 |

---

## Execution handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-12-web-react.md`.

**两种执行方式：**

1. **Subagent-Driven（推荐）** — 每个 Task 派一个新子代理，Task 间复查  
2. **Inline Execution** — 本会话按 executing-plans 连续执行，设检查点  

选哪一种？
